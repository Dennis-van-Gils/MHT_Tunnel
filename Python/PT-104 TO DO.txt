PT-104 mechanism

UDP packets can arrive asynchronously!

UDP_listener on Qtimer1 ~ 100 ms
  Every update:
    Recv UDP packet
    On recv time-out: pass silently
    Else: Put in queue
    
UDP_packet_processor on QTimer 2 (user set)
  Every update:
    Process queue
    Update state variables